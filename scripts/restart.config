% Set the configuration values and execute the below command so that git excludes your changes to this file:
% git update-index --skip-worktree FILE_PATH
% You can undo this action by running:
% git update-index --no-skip-worktree FILE_PATH

new_logs_subdirectory=
